# Dossier data/

Ce dossier contient les données utilisées pour l'analyse.

Les données brutes doivent être placées dans :

- `data/raw/` : données initiales (non nettoyées).
- `data/processed/` : données prétraitées générées par les notebooks.

Les fichiers ne sont **pas versionnés** (RGPD).

